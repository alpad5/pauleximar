# Railway Postgres connection string.
# In Railway: create a Postgres service, then copy the "Postgres Connection URL" from its Variables tab.
DATABASE_URL=postgres://user:password@host:port/database

# Optional: comma-separated allow-list for CORS / origin checks. Leave blank in dev.
# ORIGIN=https://your-app.up.railway.app
